APP_NAME=Medication Reconciliation Service
ENVIRONMENT=dev
MONGODB_URI=mongodb://localhost:27017
MONGODB_DB=med_reconciliation
CONFLICT_RULES_PATH=config/conflict_rules.json

# Production example (do not commit real secrets):
# MONGODB_URI=mongodb+srv://<db_user>:<url_encoded_db_password>@<cluster-host>/med_reconciliation?retryWrites=true&w=majority
# MONGODB_DB=med_reconciliation
# ENVIRONMENT=production
